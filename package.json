{
  "name": "blobbudget",
  "version": "0.1.0",
  "description": "Local-first repo bloat budget scanner for oversized blobs, fixture creep, binaries, duplicates, and package payloads.",
  "type": "module",
  "bin": {
    "blobbudget": "dist/src/cli.js"
  },
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/tests/*.test.js",
    "pretest": "npm run build",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm test && npm run check && npm run build && npm run smoke && npm run package:smoke"
  },
  "keywords": [
    "git",
    "repository",
    "bloat",
    "cli",
    "ci",
    "budget"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/blobbudget.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/blobbudget/issues"
  },
  "homepage": "https://github.com/rogerchappel/blobbudget#readme"
}
